# General Configuration
SCHEDULE=0 * * * * # Cron schedule for the scraper
DATA_DIR=/path/to/data # Directory for storing data
HISTORY_FILE=history.json # Filename for storing history

# Notifier Configuration
NOTIFIER_DISCORD_ENABLED=false # Enable Discord notifier
NOTIFIER_DISCORD_WEBHOOK=your_discord_webhook_url # Discord webhook URL

# HTTP Configuration
HTTP_REQUEST_TIMEOUT=10s # Timeout for HTTP requests (in seconds)
